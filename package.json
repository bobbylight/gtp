{
  "name": "gtp",
  "version": "2.0.6",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/bobbylight/gtp.git"
  },
  "main": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "scripts": {
    "clean": "rimraf lib coverage doc",
    "lint": "eslint --ext .ts src/",
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "doc": "typedoc --plugin ./typedoc-class-name-fixing-plugin.js --excludeExternals --out doc/ --name gtp --exclude **/*.spec.ts src/"
  },
  "files": [
    "lib/"
  ],
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@typescript-eslint/eslint-plugin": "^4.28.0",
    "@typescript-eslint/parser": "^4.28.0",
    "canvas": "^2.8.0",
    "eslint": "^7.29.0",
    "jest": "^27.0.6",
    "rimraf": "3.0.2",
    "ts-jest": "^27.0.3",
    "typedoc": "0.21.1",
    "typescript": "4.3.4"
  }
}
